# dependencies
node_modules
.pnp
.pnp.js

# next.js
.next/
out/
build

# production
dist

# misc
.DS_Store
*.pem
.idea
.vscode

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# payload
media

# The 3 compressed demo videos (~7.4MB total) ARE committed so they ship with
# the deploy and play without depending on a CDN env var. (NEXT_PUBLIC_MEDIA_BASE_URL
# can still override to a CDN if set; empty -> local /baserate/videos/ path.)
# uncompressed source masters — never commit
public/baserate/videos/originals/
